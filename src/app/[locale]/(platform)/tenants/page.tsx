import { PlatformPlaceholder } from "@/features/platform/components/platform-placeholder";

export default function PlatformTenantsPage() {
  return (
    <PlatformPlaceholder
      title="Tenants"
      subtitle="Alle Organisationen tenant-übergreifend verwalten — anlegen, Module, Mitglieder."
      phase="P2"
    />
  );
}
