import { PlatformPlaceholder } from "@/features/platform/components/platform-placeholder";

export default function PlatformProvisionRequestsPage() {
  return (
    <PlatformPlaceholder
      title="Tenant-Anfragen"
      subtitle="Provisioning-Anträge für neue Organisationen — genehmigen oder ablehnen."
      phase="P6"
    />
  );
}
