import { PlatformPlaceholder } from "@/features/platform/components/platform-placeholder";

export default function PlatformJoinRequestsPage() {
  return (
    <PlatformPlaceholder
      title="Anfragen"
      subtitle="Beitritts-Anfragen tenant-übergreifend (read-only) — Freigabe erfolgt beim Tenant-Admin."
      phase="P5"
    />
  );
}
